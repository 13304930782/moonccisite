const {test}=require('node:test'),assert=require('node:assert/strict'),ts=require('typescript'),fs=require('fs');const m={exports:{}};new Function('exports','module',ts.transpileModule(fs.readFileSync('src/app/lib/articleAge.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText)(m.exports,m);const {isArticleOld,stripLegacyAgeNotice}=m.exports;
test('six calendar months uses Shanghai date and clamps month-end',()=>{assert.equal(isArticleOld('2026-03-18',new Date('2026-09-17T15:59:59Z')),false);assert.equal(isArticleOld('2026-03-18',new Date('2026-09-17T16:00:00Z')),true);assert.equal(isArticleOld('2025-08-31',new Date('2026-02-28T00:00:00+08:00')),true);assert.equal(isArticleOld('2026-04-07 00:33:08',new Date('2026-09-18T00:00:00+08:00')),false);for(const s of ['',null,'invalid','2026-02-31','2027-01-01'])assert.equal(isArticleOld(s,new Date('2026-09-18T00:00:00+08:00')),false);});
test('remove generated notice once from excerpt and body, retain actual content',()=>{const notice='提醒：本文最后更新于2025-10-20 15:56，文中所关联的信息可能已发生改变，请知悉！';assert.equal(stripLegacyAgeNotice(notice+'\n\n一、数组'),'一、数组');assert.equal(stripLegacyAgeNotice('提醒：请先备份数据。'),'提醒：请先备份数据。');assert.equal(stripLegacyAgeNotice('正文\n'+notice),'正文\n'+notice);});
