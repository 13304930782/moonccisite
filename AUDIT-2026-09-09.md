# 全站代码审计与修复记录（2026-09-09）

## 结论与范围

本次对工作区的前端、后端、SQL 迁移、后台任务、部署脚本和依赖进行了文件清单扫描，并对认证、权限、内容可见性、媒体管理、邮件、电费隔离、天气配额、发布同步等关键路径进行了人工审阅与回归验证。保留并整合了前序任务尚未提交的内容平台、电费、天气和界面改动。

没有确认 P0 级问题；下表中确认的 P1 缺陷均已修复。P2/P3 中的扩展优化列为后续计划，不作为“已经完成”的工作。此次结论不等于没有其他缺陷，也不代表生产服务器已完成部署、渗透测试或容量测试。

参考任务「制定 moonccisite 开发计划」已通过任务读取接口查询；接口返回的最近记录没有消息正文，因此具体结论以当前代码、实际响应和测试结果为依据。

分级：P0 为可直接造成全站接管、重大数据破坏或全站持续不可用的已确认问题；P1 为认证、隐私、权限或核心操作可靠性缺陷，合并前修复；P2 为容量、维护和体验改进；P3 为长期工程优化。npm 的 critical/high 等级不直接等同于网站 P0。

## 已确认并修复

| 编号 | 等级 | 触发与影响 | 修复及证据 |
| --- | --- | --- | --- |
| AUTH-01 | P1 | `/auth/me`、登录、退出共用 20 次/15 分钟限流，刷新耗尽额度后退出被 429 拦截；全局额度也能拦截退出 | 认证限流仅覆盖凭据相关写接口，退出免全局额度但保留 CSRF，并有独立的每令牌 120 次/分钟防滥用额度；`auditHttp.test.js` 耗尽全局额度后验证退出仍成功 |
| AUTH-02 | P1 | Nginx 误返回 HTTP 200 HTML 时 API 客户端当作成功，界面声称退出但 Cookie 仍在 | 非 JSON 响应作为错误；React 与真实浏览器均验证“200 HTML 不等于退出成功” |
| AUTH-03 | P1 | 数据库异常被后端转换为 401，前端任何异常都会清用户，造成假退出 | JWT 失效返回 401，数据库异常返回 503；前端仅在 401/403 清会话，保留临时失败时已有用户 |
| AUTH-04 | P1 | 退出只删除 Cookie，复制的 JWT 在过期前仍可重放；重置密码不撤销旧会话 | 新增持久化撤销表和用户失效时间；先撤销再清 Cookie；重置事务同时更新密码、撤销旧会话和解锁；真实 MySQL 验证重放、重复退出、单次重置令牌及重新登录 |
| AUTH-05 | P1 | 登录请求与退出并发时，晚到的登录响应可能重建会话；管理员登录绕开统一上下文 | 串行处理认证变更；退出等待正在进行的登录响应后撤销 Cookie；版本号丢弃陈旧查询；管理员登录使用同一上下文；测试覆盖并发、跨标签页和刷新 |
| AUTH-06 | P1 | 重置密码没有注册同等强度校验；bcrypt 超过 72 字节会截断 | 重置与注册共用密码规则并限制 72 字节；注册校验邮箱；弱密码不消费重置令牌 |
| AUTH-07 | P1 | 手工认证限流信任任意 `CF-Connecting-IP`、`X-Real-IP`，易被伪造 | 使用 Express `req.ip` 与明确的可信代理；默认只信任本机代理、HTTP 服务只监听回环地址 |
| AUTH-08 | P1 | 弱或示例 JWT 密钥可让误配置站点启动；旧认证副本缺少新撤销逻辑 | 启动拒绝短于 32 字符及已知占位密钥；旧入口改为统一认证模块的兼容导出 |
| HTTP-01 | P1 | Express 4 的多个 async 路由拒绝 Promise 后不转交错误，请求悬挂 | 统一 async Router 适配；全局 JSON 错误响应；故障注入验证设置接口及时返回 500；非法 JSON/过大请求返回 400/413 |
| HTTP-02 | P1 | 全局异常只记日志继续运行，进程可能处于损坏状态 | 仅独立启动时注册致命异常处理，以非零状态退出交 PM2 重启；正常请求异常走路由错误处理 |
| PRIVACY-01 | P1 | 草稿文章被隐藏，但评论独立接口仍公开；知道 ID 可以往草稿写评论或点赞 | 评论读取校验文章可见性；只有文章作者及管理员能读取草稿评论；创建/点赞限定已发布文章；真实数据库验证 |
| PRIVACY-02 | P1 | 管理响应、草稿和个性化评论缺少统一缓存策略 | 认证及个性化响应设置 `private, no-store`，并按 Cookie/Authorization 声明 Vary |
| MEDIA-01 | P1 | 编辑可以改名或删除共享媒体，从而影响其他作者文章 | 共享媒体变更限定 owner/admin；同步前端导航及路由；编辑仍可在写文章时上传图片；验证编辑删除共享媒体返回 403 |
| DB-01 | P1 | 当前 schema 已包含后续迁移，但初始化只记录前 7 项；再次 migrate 报重复 `scope_key` 列 | 初始化记录与当前快照一致的 12 项基线；全新 QA 库初始化后迁移无待执行项；已有数据库仍使用增量迁移 |
| DEP-01 | P1 | 扫描发现前端 8 项、后端 4 项依赖告警，包括开发工具链、编辑器净化和请求解析相关漏洞 | 保持 Vite 6/Express 4 主版本升级兼容安全版本；锁定 DOMPurify 3.4.15、qs 6.16.0；前后端 npm audit 均为 0 |
| DEP-02 | P1 | Toast UI ESM 源码内嵌 DOMPurify 2.3.3；仅 override 依赖不会替换实际运行代码 | Vite 适配器替换完整内嵌净化实现，覆盖内部粘贴/DOM fragment 路径；禁用编辑器预打包旁路；上游结构变化时构建明确失败；浏览器验证正常编辑及危险标签/事件移除 |
| MAIL-01 | P2 | 订阅确认接口忽略邮件函数 `{sent:false}`，可能声称发送成功 | 显式检查投递结果并返回 503，不把未发送当成功 |
| CONFIG-01 | P2 | `CORS_ORIGINS` 配置存在但 CORS 中间件仍硬编码域名 | CORS 读取环境配置，与 CSRF 源白名单的配置用途保持清晰 |
| QA-01 | P2 | 普通测试命令默认跳过数据库测试，没有统一合并检查 | 新增 React、HTTP、浏览器回归和隔离 MySQL 运行器；GitHub Actions 运行构建、单元测试、浏览器、12 项 MySQL 测试及依赖审计 |
| QL-01 | P1 | GitHub CodeQL 补充发现邮件链接尾部标点和电费邮箱校验的正则存在多项式回溯风险 | 标点改用线性逆向扫描；邮箱限制 254 字符并使域名分段不重叠 |
| QL-02 | P2 | DMG 上传使用 multer 提供的文件路径，CodeQL 将其按请求数据追踪到文件操作 | 改为服务端随机文件名对应的 WeakMap 路径，不从请求文件对象提取文件系统路径 |
| QL-03 | P2 | 自定义限流包装使静态分析不能识别已有额度；独立挂载路由也需要自身的保护；测试中另有过宽 URL 前缀和不区分大小写的标签断言 | 全局限流直接挂载、豁免放入官方 skip 回调；认证改用官方限流器并规范 IPv6 分组；管理员/评论/Early Access 模块增加独立 300 次/分钟保护，不与退出共享；修正测试的精确 origin/标签断言 |

关键证据文件：`src/app/context/AuthContext.tsx`、`src/app/lib/api.ts`、`server/src/middleware/auth.js`、`server/src/routes/auth-cookie.js`、`server/src/lib/asyncRouter.js`、`server/src/routes/comments.js`、`server/src/routes/upload.js`、`server/scripts/init-db.js`、`scripts/editor-sanitizer.mjs`。

## 验证结果

- 前端 `npm install`、`npm run build` 成功；保留现有视觉设计。构建仍有较大 chunk 提示，详见 P2 计划。
- 前端自动测试 2 项通过，覆盖真实 AuthContext 状态与编辑器净化适配器。
- 后端常规测试 108 项通过；默认跳过的 12 项通过独立 MySQL 运行器另行全部运行并通过，未把 skip 算作通过。
- 浏览器回归通过：HttpOnly Cookie、失败退出、成功退出、跨标签页同步、刷新不复活、编辑器安全清理和输入回写。
- 数据库验证覆盖内容发布、草稿权限、会话撤销、单次密码重置、站点设置、GitHub 同步可见性、周报重试、电费 RSS/历史/多宿舍隔离、天气配额和并发事件。
- 在 MySQL 8 隔离 QA 数据库测试，不连接生产数据库；SMTP、Google/学校等外部服务使用测试替身，没有发送真实通知。
- 全站文件扫描与敏感信息模式检查的命中均位于测试夹具。真实 `.env`、缓存、数据库备份、上传目录及构建产物排除提交；该模式扫描不等同于完整历史秘密扫描。
- 依赖告警以本次 npm 官方审计结果为准；未使用跨主版本强制升级消除告警。编辑器额外检查实际运行代码，不能只看 npm 数字。

## 后续修复/优化计划

| 等级 | 工作 | 优先顺序与验收 |
| --- | --- | --- |
| P0 | 当前无已确认项 | 如上线后出现接管、破坏或持续不可用，立即停发版、隔离影响并回滚/前向修复 |
| P1 | 本次已确认缺陷均已修复 | 发布时先备份并迁移，再更新 API/worker/前端；执行下面退出验收，不能只上传 dist |
| P2 | 入口包与编辑器包分别约 599 KB、586 KB，后台部分页面仍静态导入 | 下一轮优先做后台路由懒加载与包体预算；对比压缩体积和首屏加载时间，保证编辑器及所有路由回归通过 |
| P2 | 管理列表及媒体扫描在大数据量下的容量验证 | 用脱敏/合成数据评估查询时间，再决定分页、索引与增量扫描；不把未经测量的慢查询猜测列为已证实故障 |
| P2 | 生产运行态验收 | 站长在实际 Nginx/HTTPS/代理/CDN 配置下复验 Cookie 域、缓存、邮件与学校 API；本次 Git 合并不会自动部署宝塔 |
| P3 | 更全面的类型检查、变更影响测试与依赖更新流程 | 分阶段处理历史类型债务；建议仓库将两个 Site checks job 设为 main 必须通过的检查（本次不擅自修改保护规则） |

## 防止退出问题复发的发布门槛

每次认证、API 客户端、代理、Cookie、依赖或用户权限相关变更必须通过 CI。验收至少包含：登录后刷新、连续刷新超过认证阈值、退出接口断网/503/200 HTML、重复点击退出、迟到的 me/登录响应、两个标签页、退出后刷新、旧 JWT 重放、密码重置后旧会话拒绝。Cookie domain/secure 配置变化需在真实 HTTPS 环境另测。

复现命令（项目根目录）：

```bash
npm ci
npm ci --prefix server
npm test
npm test --prefix server
npm run build
npm audit --audit-level=moderate
npm audit --prefix server --audit-level=moderate
npx playwright install chromium
node scripts/test-browser.cjs
```

MySQL 测试专用实例需本地 root 空密码，默认端口 33079；CI 通过 `TEST_DB_PORT=3306` 使用临时服务。运行 `node server/scripts/test-integration.js` 会创建/修改脚本中固定的 QA 库，不接受生产库名称。Windows 已安装 Chrome 时可设置 `TEST_CHROME_PATH` 使用该可执行文件。勿把测试夹具部署为生产应用入口。
