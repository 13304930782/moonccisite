-- This file is intentionally harmless.
-- The migration runner creates schema_migrations automatically before applying files.
-- Keep this as the first recorded migration so the migration system itself has a history entry.

SELECT 1;
