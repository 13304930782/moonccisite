# GitHub 安全告警复核（2026-09-09）

基线：`077e9761443dc02f6067ad571c9f242715a42956`（main）。逐条读取 CodeQL 分析 `1742794205` 的 SARIF 数据流及对应源代码。

## 需要修复

| 告警 | 优先级 | 判断和修复 |
| --- | --- | --- |
| #7 settings.js 邮件地址正则 | P2 | 管理员自定义邮件接口允许较长的请求字段进入回溯正则；在匹配前拒绝超过 254 字符的地址。 |
| #8 upload.js 媒体改名正则 | P2 | 去除首尾标点的正则可能对大量重复标点反复扫描；使用索引和线性扫描，并保留现有命名规则。 |

两条告警虽然标记 High，但实际入口需要管理员权限且请求体上限为 1 MB，因此按实际利用条件列为 P2；不作为误报关闭，由修复后的 CodeQL 扫描确认。

## 确认误报

| 告警 | 位置 / 行为 | 核实依据 |
| --- | --- | --- |
| #46 | AdminContentPage.tsx 图片预览 | 数据从 input.value 经表单状态、safeImageSrc 的相对路径分支进入 React img.src；没有 HTML 字符串拼接。控制字符、反斜杠、协议相对 URL 被拒绝，绝对地址只允许 HTTP/HTTPS。React 属性转义阻断引号注入。 |
| #32–33 | applications.js 查询、提交申请 | 均挂载于 index.js 全局 /api 限流之后，不满足 weather/logout 豁免条件。 |
| #34–39、#41 | posts.js 分类、标签、列表、详情、增删改 | 同上。 |
| #40 | settings.js 自定义邮件 | 同上，另外有管理员权限与每日发送量检查。 |
| #42–45 | upload.js 媒体列表、编辑、改名、压缩 | 同上，媒体修改还需要管理员权限。 |
| #1 | upload.js 删除临时图片 | SARIF 的三条来源均是 Multer req.file.path 或压缩函数中的同一路径；磁盘存储目录固定为 uploads，名称由服务器生成，原始名称只用于受控扩展名。 |
| #2 | upload.js 读取图片大小 | 来源为相同的 Multer 服务端生成路径。 |
| #3 | upload.js 原图扩展名修正 | 源路径来自 Multer；目标名称使用 basename 和内容检测映射出的固定扩展名。 |
| #4–6 | upload.js 原图/GIF/ICO/压缩前大小读取 | 路径来自相同的 Multer 磁盘存储回调，非客户端可指定的文件路径。 |

以上 21 条按 false positive 关闭，保留 CodeQL 扫描和实际防护。判断仅适用于当前实现；改变 Multer 存储回调、URL 校验或路由挂载顺序时须重新复核。

## 验证与其他扫描

- 回归测试逐一请求 14 个被标记的接口，确认全局额度耗尽后返回 429，退出登录仍可成功。
- 40 万个重复字符的邮件地址被拒绝；80 万个标点字符的改名输入能及时完成处理。
- 图片地址测试覆盖危险协议、相对地址以及引号/事件属性注入，React 渲染不会创建事件属性。
- 本地前端 3 个测试通过；后端 109 个测试通过，12 个需要真实 MySQL 的用例留给 CI 执行。生产构建通过。
- Dependabot：0 open，56 fixed；不是手工忽略。密钥扫描：0 open。
- 自动扫描结果不是无漏洞保证。上述修复部署到生产前，线上仍使用原正则实现。

本次不修改数据库迁移。不要重新运行旧的完整审计部署包，以免覆盖线上已恢复的历史迁移文件。
