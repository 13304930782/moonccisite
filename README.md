# mooncci 全栈博客

## 前端启动
```bash
npm install
npm run dev
```

## 前端打包
```bash
npm run build
```

## 后端启动
```bash
cd server
npm install
cp .env.example .env
npm run dev
```

默认后端端口 `3001`，API 基路径 `/api`。
