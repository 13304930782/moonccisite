# mooncci 天气小球

前台右下角接入原版 [grok-ball](https://github.com/tycoding/grok-ball)，固定版本见 `src/vendor/grok-ball/UPSTREAM.md`。运行时、几何、原版表情与动作未改动。原版资源随站点打包，不从外部 CDN 加载；MIT 许可保留在源码及 `/licenses/grok-ball.txt`。

## 行为

- 城市由每位访客选择，可手动搜索城市（同名结果显示国家/省市），也可点击“使用当前位置”并授权浏览器定位。首次访问不自动弹授权、不默认选取城市。未选择城市时显示待机，后台临时心情仍有效。
- 定位仅获取一次，不持续追踪；识别区县时原始坐标在浏览器内立即四舍五入到 0.001 度（约百米），服务端使用独立精度的逆地理编码缓存。天气查询与浏览器持久化仍降为 0.1 度，仅保存选定名称及天气区域；不保存街道、未取整坐标。使用高精度一次定位，缓存位置不复用；浏览器报告误差大于 1 千米时提示手动选择，不将不可靠位置自动识别成某个区。选择在当前浏览器 localStorage 保存 30 天，可在“切换城市”中清除。不与登录账号、IP 建立关联；位置通过本站 POST 请求体传递，不放入查询 URL。
- 按所选地区使用当地当日预报：中国大陆使用高德天气（Asia/Shanghai），其他覆盖区域使用 Open-Meteo（自动识别时区）。每天首次请求取一次日预报；缓存按来源、行政区及近似经纬度分区，写入现有 `site_settings`。MySQL 命名锁与每区域请求合并避免重复访问；失败至少间隔 15 分钟重试，旧日期不冒充今天，不同城市不串数据。
- 中文城市名称优先查询已配置的高德或 Nominatim 行政区数据，支持县、县级市及区；其他名称及失败回退使用 Open-Meteo Geocoding（GeoNames 数据），结果缓存 1 小时；手动提交搜索，不随每个按键请求。定位授权后调用已配置的城市服务将大致坐标解析为城市及省份。优先返回中国县级市/区县名称，同时保留省份与上级城市说明，不保留街道地址；无法识别地区时暂不猜测天气来源，提示重试或手动选择。近似位置靠近行政边界时可手动纠正。未授权/超时可继续手动搜索。
- 天气类别确定适合的原版表情范围，同类天气按日轮换。例如晴天可为开心、满意、好奇、害羞。原版单个表情本身也轮换多个眼形及动作；不承诺 31 个互不重复的开心表情，不添加自定义表情。
- 鼠标移动时眼睛跟随；按住小球划动（手机揉动）会短暂开心并轻弹，之后恢复当天状态。键盘用户可以在卡片内点击“摸摸”。原版睡眠等状态按原版行为处理。
- 点击展开心情、所选城市当日预报和数据来源；收起状态记在当前浏览器会话。后台页面不显示悬浮球。页面隐藏或系统偏好减少动态效果时暂停动画。
- 后台站点设置 → 天气小球：站长/管理员可指定 16 种适合持续显示的原版心情，默认 1 天，可设 1～365 个整数天。从保存时起按 24 小时/天计时，到期自动恢复各访客所选城市的天气模式，也可提前恢复。再次保存重新计时。
- 前台每分钟查询本站缓存接口，以接收后台变更；不会每分钟访问天气提供商。手动状态到期也会触发刷新。多 API 进程时管理设置缓存最长 30 秒。

## 接口与持久化

- `POST /api/weather-mood`：`{"location":{"name":"城市名","region":"省份","latitude":30.3,"longitude":120.2}}`；坐标经边界校验后再降精度。未选城市传 `location:null`。每浏览器/已验证账号每分钟最多 30 次；响应 `Cache-Control: no-store`。
- `GET /api/weather-mood`：兼容旧客户端，返回未选择城市及后台临时心情，不再默认查询兴城。
- `POST /api/weather-mood/cities`：`{"query":"杭州"}`，每浏览器/已验证账号每分钟最多 10 次；访问服务端配置的行政区搜索服务或固定的 Open-Meteo 地址，不接受客户端传入上游 URL。
- `POST /api/weather-mood/locate`：授权后提交近似坐标，返回城市名称；复用城市搜索限流、全局 CSRF、no-store。Nominatim 搜索与定位识别共用 MySQL 锁，合计限至每 1.1 秒一次，同区域结果缓存 24 小时、最多 128 区域，不随天气轮询或刷新重复查询。
- `GET /api/admin/weather-companion`：读取管理设置。
- `PUT /api/admin/weather-companion`：`{"emotionId":"10","durationDays":1}`。
- `DELETE /api/admin/weather-companion`：恢复天气模式。

后三个管理接口复用 JWT Cookie、有效用户检查和管理员权限；所有 POST/PUT/DELETE 请求受现有全局 CSRF 保护。天气缓存使用内部键 `weather_mood:v2:<区域哈希>`，不保存访客提供的城市昵称；临时设置仍使用 `weather_companion_override:v1`，不新增数据库迁移。旧固定城市缓存不再读取。不发送邮件，不调用电量学校接口。

大陆天气来自[高德天气](https://lbs.amap.com/api/webservice/guide/api/weatherinfo)，复用现有 Web 服务 Key；其他区域来自 [Open-Meteo](https://open-meteo.com/)。前台按实际来源显示署名；城市识别来源与天气来源分别标明。Open-Meteo 数据按 CC BY 4.0 标注来源。

## v11 北京服务器部署（动画性能优化）

Windows PowerShell：

```powershell
Set-Location 'C:\Users\Administrator\Documents\mooncci site'
scp .cache/mooncci-weather-companion-20260908-v11.tar.gz root@182.92.179.81:/root/
```

服务器 SSH：

```bash
mkdir -p /root/mooncci-weather-companion-20260908-v11
tar -xzf /root/mooncci-weather-companion-20260908-v11.tar.gz -C /root/mooncci-weather-companion-20260908-v11
```

已配置高德 Key 的服务器不需重复配置。首次配置才在服务器 SSH 执行：

```bash
bash /root/mooncci-weather-companion-20260908-v11/configure-weather-amap.sh
```

按提示粘贴高德「Web 服务」Key，输入不回显，不进入命令历史。脚本实际检查行政区查询及逆地理编码，通过后备份原 `.env` 并写入 `MOONCCI_CITY_PROVIDER=amap`、`AMAP_WEB_SERVICE_KEY`。不需要 JS API 安全密钥；不将 Key 写入前端/发布包。失败不会修改原配置，请按错误检查 Key 类型、权限、额度或服务器 IP 白名单。

看到“高德校验通过 / 服务器配置已保存”后，在服务器 SSH 执行：

```bash
bash /root/mooncci-weather-companion-20260908-v11/deploy-weather-companion.sh
```

部署脚本要求已配置高德，先实际查询北京当日预报验证天气权限与数据，失败立即停止；通过后才备份源码与静态入口、重启 API 并发布前端。未应用的 202609080001 互动记录表迁移会自动执行，已应用则跳过；v8 没有新迁移，无需 npm 安装或 worker 重启。刷新网页、打开小球，旧“当前位置附近”记录会用已保存的大致坐标自动识别名称，不再次申请系统定位权限。识别失败显示原因和重试按钮，也可手动选城市。

恢复包内源码构建：`npm run build`，然后 `python scripts/build-weather-companion-package.py`。

## 验证

`node --test server/test/weatherMood.test.js server/test/weatherMoodRoutes.test.js server/test/weatherLocation.test.js` 验证天气代码/缺失数据、北京时间跨日、每日缓存、并发请求、重启后复用、失败节流、临时状态时长/到期、晴天原版表情轮换、管理员权限及恢复天气接口。上游网络请求与数据库在路由测试中隔离。

早期本地预览使用隔离账号及实际公开天气/城市接口；v8 高德测试使用隔离响应样例，真实服务端 Key 留给部署前检查。测试覆盖城市隔离、近似坐标、跨日/夏令时、缓存和权限。浏览器检查手动城市搜索、选择、刷新保持与小球卡片；未使用真实设备定位，也不是 iOS 真机测试。定位需要 HTTPS 或 localhost，拒绝授权时会提示手动选择。

城市反向识别使用 [Nominatim](https://nominatim.org/release-docs/latest/api/Reverse/)，按[使用政策](https://operations.osmfoundation.org/policies/nominatim/)识别客户端、缓存、共享限流并显示 OpenStreetMap 署名。服务端可通过 `MOONCCI_REVERSE_GEOCODING_URL` 切换兼容的 HTTPS 服务，无需更新前端；默认服务无需密钥。定位权限由浏览器和系统共同控制，HTTPS 正式站点才可申请（localhost 除外）；已拒绝时需在浏览器站点权限中重新允许。

县级市验证：真实搜索“兴城市”返回辽宁省兴城市；同名行政边界与城市中心去重并优先城市中心。定位返回 district=兴城市、city=葫芦岛市 时显示兴城市；不把街道名当城市。Nominatim-compatible 服务须提供同目录的 reverse 和 search 接口。

跨城市验收：真实接口检查兴城市、江阴市、义乌市、长汀县、北京、上海，以及南山区和朝阳区的跨省同名结果；修正直辖市被标为 state 的情况、搜索名称与嵌套 address 不一致的情况。用 ISO 3166-2 省级代码补足省份；坐标接近的同城边界/中心去重，远距离同名地区保留。未加入任何县市专用别名映射。不保证第三方数据涵盖所有地名；失败保留手动搜索与原搜索源回退。

## 高德接入实现及验证边界

- 配置 `MOONCCI_CITY_PROVIDER=amap` 后，定位识别与中文行政区搜索使用 `restapi.amap.com`；此为 v4 的城市识别接入；v8 已将大陆天气切换到高德。英文等搜索保留 GeoNames 路径，国内城市无需 Nominatim 连通性。
- 行政区搜索获取一次全国省、市、区县树，内存缓存 24 小时并合并并发请求；进程重启后首次搜索重新获取。按层级保留真实父级路径、以 adcode 区分同名地区，支持县级市、县、区、直辖市、省名加县市名。街道不作为县市选项。不新增持久化全国行政区数据库。
- 定位结果缓存大致坐标网格 24 小时，沿用现有 site_settings 和 MySQL 锁；缓存条目限制 128 项、JSON 45 KB，Key 和街道地址不进入缓存。高德 GCJ-02 与天气 WGS84 坐标在服务端转换，所用 coordtransform 固定版本和 MIT 许可在 server/src/vendor/coordtransform。小球不跟踪或保存精确位置。
- 旧记录自动补查城市名，识别失败不会静默消失；Key 无效、平台不匹配、额度、白名单、网络问题返回可操作的错误提示，不返回 Key 或包含 Key 的 URL。
- 代码/构建及浏览器升级流程已验证；高德 API 在本地测试中使用隔离响应，真实 Key/北京网络由 configure-weather-amap.sh 现场验证。未声称 iOS 真机或用户实际定位已通过。

官方接口参考：[行政区域查询](https://lbs.amap.com/api/webservice/guide/api/district)、[逆地理编码](https://lbs.amap.com/api/webservice/guide/api/georegeo)。


## v5 区县精度与 30 天选择持久化

- 修正定位先舍入到 0.1 度导致数公里偏移的问题。区县识别单独使用 0.001 度坐标，保留 WGS84 → GCJ-02 转换；新缓存前缀不会命中旧的粗网格区县结果。天气缓存保持原来的粗网格并按每次请求返回选择的名称。
- 新存储键 `mooncci-weather-location-v2` 记录手动/定位来源、选定时间及到期时间。刷新及天气轮询不续期、不自动定位，也不覆盖手动选择。旧记录首次读取迁移；清除和过期不会从旧键复活。失败明确显示保存提示。仅同一浏览器、同一网站源有效；清除网站数据或结束无痕会话可能提前丢失。
- 已经保存错误区名的访客应更新后重新定位或手动选择一次；仅凭旧的粗坐标不能推断真实区县。30 天到期后提示重新选择，不自动调用系统定位。
- 后端全套测试 94 项，87 通过、7 项需要独立数据库环境的集成测试跳过；TypeScript 检查与 Vite 构建通过。新增测试覆盖邻近坐标缓存隔离、旧缓存失效、30 天边界、读取不续期、旧格式迁移、清除后不恢复、存储不可用。
- 城市接口验证使用隔离数据，不能证明真实沈北设备定位精度；未进行 iOS 真机定位验收。

本地浏览器已验证：从旧选择进入 → 手动搜索并选择“沈北新区” → 刷新页面 → 打开卡片，天气标题及城市栏均保留“沈北新区”。区县搜索是隔离响应，天气请求使用现有天气服务。


## v6 全站今日互动与短暂生气

- 小球卡片显示“今日被摸 / 今日被打”，统计范围为全站。北京时间每天 00:00 切换日期，数据库历史保留，不与天气城市时区混用；显示中的统计每 30 秒刷新，操作后立即取服务端结果。
- 摸摸按钮或一次按住揉动达到 28 像素累计路径，计一次 pet。一轮按住不重复计数。普通单击延迟约 0.36 秒打开/关闭卡片，键盘激活立即打开。
- 同一个小球上两次连续双击（每对间隔不超过 350 毫秒、两对完成间隔不超过 1 秒），计一次 hit，不额外增加被摸次数或反复开关卡片。鼠标与触摸点击使用同一手势逻辑。
- 使用原版 21 号生气表情，持续 3.5 秒后恢复当前天气/后台表情。摸摸使用原版 10 号开心表情；生气期间摸摸仍计数，但先完成短暂生气。不同访客的表情反馈互不干扰，计数共享。
- 新增 `weather_companion_interactions` 表：仅保存随机互动编号、pet/hit、北京时间日期和 UTC 时间，不额外记录账号、IP、城市或设备标识。主键去重避免并发/重试重复计数；服务端决定日期，不接受客户端传来的次数。每 IP 每分钟最多 120 次统计相关请求，复用现有 CSRF。
- `GET /api/weather-mood/interactions` 读取今日统计；`POST` 传 `{id: UUIDv4, kind: 'pet'|'hit'}` 记录一件互动，均 no-store。请求失败不伪装成已计数，会显示提示；没有可靠统计时显示破折号。
- v6 发布包包含新 SQL。部署脚本先验证历史迁移校验和，仅允许本次迁移待执行；遇到其他待执行迁移则停止，不擅自发布。先建表再更新代码，随后只读检查互动接口。代码回滚不删除新增表或记录。无需重新填写高德 Key。
- 已在隔离的本地 MySQL 8.0 上执行建表、重复执行建表、并发插入、重复事件、重新连接读取和跨北京时间零点测试。浏览器实际验证摸一次计 1、两次双击计 1 且短暂生气、恢复与刷新后保留；移动设备手势尚未真机验收。

v6 验证结果：开启 `COMPANION_INTEGRATION=true` 后全套 98 项测试中 91 通过、7 项其他集成测试跳过；TypeScript 检查和生产构建通过。


## v7 小球卡片排版整理

- 主卡片移除城市保存时长和互动玩法的常驻说明；计数与错误提示保留。
- 手动选择搜索结果、或用户主动定位成功后，先显示地区确认弹窗，说明此浏览器保存 30 天。取消不改变选择，确认才保存；历史占位记录自动补名称不弹窗打扰。
- 摸摸旁的问号提供按需玩法提示，支持悬停、键盘聚焦与触屏点击；移开、点击外部或 Escape 关闭。提示浮在卡片外层，桌面空间允许时放在卡片旁，窄屏放在按钮上方。
- 高德地图与 Open-Meteo 署名合并到底部一行，保留可点击来源；极长的其他来源署名允许自然换行以避免溢出。
- 仅调整前端，v6 的数据库、全站计数及生气逻辑保持不变。使用 v7 部署包；已部署 v6 的互动表迁移自动跳过，无新迁移。

v7 验证：TypeScript 与生产构建通过；本地窄屏深色预览检查来源同排、玩法浮层及地区确认/取消/Escape，桌面 Edge 浅色检查提示侧置与键盘聚焦。未进行手机真机验收。

## v8 天气来源分流

- 中国大陆按所选城市的行政区代码请求高德预报；国外及港澳台覆盖区域继续使用 Open-Meteo，不按访客 IP、网站服务器位置或语言判断。
- 新城市选择保留国家代码、行政区代码及城市来源，仍保存 30 天。旧版已选城市缺少行政区代码时先通过原城市名称查找，必要时再逆地理识别。
- 来源与行政区参与缓存键，旧的单源缓存不再复用。大陆服务失败不隐式访问国外天气服务，失败缓存保持 15 分钟，次日预报与原版心情轮换策略不变。
- 高德温度按“日 / 夜”展示，Open-Meteo 保留最低 / 最高范围；缺失温度为 —。不把高德白天、夜间温度伪装成极值。
- 地图与天气署名沿用紧凑同行布局，名称跟随实际服务变化；GeoNames 城市结果显示 GeoNames。
- 部署先用现有 Key 检查高德天气权限和当日预报，不输出 Key。无需额外 Key、依赖安装或新数据库迁移。

验证：服务端 103 项测试中 95 项通过、8 项依赖专用数据库的集成测试按条件跳过；新增测试覆盖地区分流、相邻区县缓存隔离、重启复用、失效数据、零温度、无跨源回退及城市保存元数据。TypeScript 检查与生产构建通过。浏览器本地预览核对高德日夜温度和紧凑来源排版，并通过实际 Open-Meteo 搜索和天气接口切换至伦敦，显示 GeoNames / Open-Meteo 及当地日期；高德预报使用隔离样例，不是生产接口实测。真实高德 Key 和服务器连通性需要由部署脚本现场检查。

## v9 上游预算与校园网限流

截图额度为天气每月 5,000 次、基础 LBS 每月 150,000 次、相关接口每秒/并发上限 3。本站保守使用：

| 上游组 | 每天 | 每月 |
| --- | ---: | ---: |
| 高德天气 | 140 | 4,500 |
| 高德逆地理与行政区查询合计 | 3,500 | 120,000 |
| Open-Meteo 天气 | 1,000 | 20,000 |
| 国外城市搜索/识别 | 500 | 10,000 |

国外源限制是本站自设预算。预算按北京时间日/月切换；各组最少间隔 600ms 发起一次请求，最多两次在途，短暂等待有长度和时间上限。只在实际发出上游请求前扣次，失败也计次，缓存命中不计次。额度耗尽可继续读取有效天气缓存，新区域显示暂不可用。上游异常不切换来源规避预算。

计数及请求占用写入现有 site_settings 的 weather_budget:v1:*，使用 MySQL 命名锁串行更新，多进程、进程重启共用；数据库故障停止新增上游调用。预算使用独立的 2 连接小池，避免天气缓存锁与预算锁争抢同一连接池。无新迁移；首次上线从本站开始计数，不能知道其他应用或旧版本已经使用的 Key 额度。独立 Key 上的外部调用不受本站预算控制。

匿名浏览器使用服务器签名、HttpOnly、SameSite=Lax、生产 HTTPS Secure 的 30 天 Cookie；不绑定 IP，不保存设备指纹。登录 JWT 验签成功后额外限制同一账号，不能仅凭客户端自填用户 ID 获取身份。天气 30 次/分钟，城市搜索与定位合计 10 次/分钟，小球查询与互动合计 120 次/分钟；每组另有宽松的同 IP 10,000 次/分钟保护。天气相关路径不再经过旧的 200 次/分钟共用 IP 限制，其他业务保持原规则。

分钟计数为当前 API 进程内存，适用现有单实例 PM2；换 IP 不重置浏览器/账号计数，但重启或多实例不是共享分钟计数。硬性上游预算始终由数据库统一执行。清 Cookie/无痕并不能保证识别成同一人，最终由全站预算限制消耗。429 响应带 Retry-After。

同一高德行政区的坐标变化不再生成新天气缓存；旧缓存键升级一次，会按新规则重新获取。部署校验以及本包 Key 配置脚本的真实高德请求也计入预算。

小球手机状态标签改为内容自适应宽度、禁止换行并居中，“生气了！”不会挤成两行。v9 不修改首页标题或后台保存的文字。


v9 验证：服务端全套 107 项中 98 项通过、9 项条件跳过；另单独启用专用 MySQL 测试，确认 30 个并发更新不丢计数、连接池重建后预算仍有效。新增测试覆盖日/月切换、失败扣次、上游并发占用、存储失败拒绝调用、同出口 205 浏览器互不误限、同浏览器换出口不重置、同账号跨浏览器限制和伪造 Cookie。TypeScript、生产构建及部署脚本语法检查通过。本地窄屏浏览器实际连续四次点击小球，截图确认“生气了！”单行居中且提示气泡分离；未进行 iOS 真机测试。


## v10 后台与手机交互修复

- 后台独立内容容器下滚超过 320px 时，右下方出现“回到顶部”；点击平滑返回当前页最上方并将键盘焦点放到标题，偏好减少动画时立即返回。移动端打开后台菜单时隐藏按钮，页面底部保留按钮空间。
- 小球仍需连续两次双击（四次点击）才记一次“打”。判定改为主指针抬起事件，兼容鼠标与触屏，不依赖合成 click 的 detail。双击间隔放宽到 450ms，两组完成时间差最多 1600ms；点击与合成 click 不重复计数，键盘单次激活仍打开卡片。
- 轻微手抖不会触发摸摸：揉动既需累计移动 28px，也需离起点至少 12px；取消触摸、多指、长按不误计点击。手机点击区域加宽，原版小球尺寸不变。
- 登录/注册及共用 AuthShell 的账户页面使用 touch-action: manipulation，移动表单文字不低于 16px，减少双击及聚焦误放大。保留双指缩放、输入选择和复制，不限制 viewport 最大缩放。离开账户页面移除专用触控样式。
- 参考 [WebKit 触控说明](https://webkit.org/blog/5610/more-responsive-tapping-on-ios/)。第三方 Google 登录 iframe 内部交互由其自身实现，本站不修改。

本次没有新数据库迁移、依赖或配置项。v10 部署包包含此前的额度保护；按上方命令部署，无需重复配置高德 Key。


v10 验证：4 项小球交互回归测试通过，包括较慢连击、轻微抖动、揉动只计一次、多指与取消事件、长按及计数幂等；TypeScript 与生产构建通过。后台独立滚动容器、焦点与减少动画分支已检查。尚未进行 iOS 真机双击/输入框缩放验收。


## v11 电量图表与小球动画性能优化

依据 2026-09-08 用户提供的 32.76 秒 Performance 录制定位：电量页面首次显示时，Recharts ResponsiveContainer 的同步 getBoundingClientRect 引发一次约 19.84ms 的 Layout。未发现超过 50ms 的主线程任务，因此本次只处理已定位开销，保持现有页面视觉及动画。

- 图表改用 ResizeObserver 已提供的 contentRect，取消初次显示时的同步尺寸测量。一次动画帧内多次尺寸变化合并，只在有效尺寸变化时更新；卸载后取消待执行工作。
- 图表类型切换保留外层尺寸观察器，仅重新播放内部图表动画；保留 180ms 淡入、380ms 折线/柱状动画、减少动画偏好和鼠标/触摸提示。
- 小球缓存上次 SVG 身体/眼睛 transform 和眼睛 path，仅跳过完全相同的属性写入；不改刷新频率、原版表情、呼吸、眨眼、鼠标跟随、互动及粒子效果。第三方修改说明见 src/vendor/grok-ball/UPSTREAM.md。
- 没有新增依赖、数据库迁移或配置项。部署包累计包含 v10 交互修复和此前额度保护；已配置高德 Key 无需再次配置。脚本仍会预检高德权限、备份和重启 API，以保持累计包部署流程一致。

验证：7 项针对性测试全部通过；TypeScript 检查和生产构建通过。两组确定性回放覆盖全部 32 个公开表情、转场、注视和特效，逐帧 SVG 输出与优化前一致：5ms 调度下 41,600 帧，属性写入由 635,539 次降至 601,151 次；60Hz 调度下 12,480 帧，由 193,029 次降至 189,203 次。该数字只衡量测试场景下的 SVG 属性写入，不能解释为页面 FPS 或耗电改善比例。

本地浏览器使用隔离电量样例，验证桌面/390px 容器、图表类型和 7/30 天切换、柱状提示；窄屏图表实际 SVG 与容器同为 295×280px，控制台无错误或警告。未进行上线后的 Performance 对照录制或 iOS 真机验收。生产构建保留已有大分块提醒，本次未扩大到编辑器拆包。

复验命令：

```bash
node --test server/test/chartPerformance.test.js server/test/grokPerformance.test.js server/test/companionInteractions.test.js
npm run build
```
