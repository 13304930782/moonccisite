# 近况评论与加载体验

- 近况列表和详情移除邮件订阅区，文章、首页和项目页保持原有订阅入口。
- 路径改变时回到页首，防止从页面下部打开近况后仍停留在订阅区位置；筛选查询参数变化不会强制滚动。
- 近况复用文章评论界面、审核、违禁词、回复、点赞、作者权限和后台管理。文章与近况的评论目标分别保存，审核邮件跳转到对应内容。
- 后台和非首页路由改为按需加载；主导航悬停、聚焦或触摸时预加载对应页面代码。
- 首次请求显示结构骨架，不生成默认文章或站点文案。成功加载的公开内容在内存中短暂缓存 30 秒，同一资源重验证不清空页面；请求 401/403/404 时移除旧内容。
- 站点公开配置在当前浏览器标签页保存至多 5 分钟的真实快照，刷新时立即显示，同时后台重新读取；私人接口和登录数据不使用此缓存。
- 手机电量图表：7 日最小宽度 560px，30 日 1320px，容器内横向滑动；隐藏图表和历史表格的滚动条，页面本身不横向溢出。

## 验证

生产构建入口 JS 从约 599 KB 降至 389 KB（gzip 从约 183 KB 降至 124 KB）。这比较入口文件大小，不代表首次访问总下载量或实测网络耗时。

`scripts/test-updates-browser.cjs` 验证拆包、配置请求挂起时保留已加载配置、底部导航后滚动复位、文章订阅保留、近况评论请求和移动图表宽度/滚动条。真实 MySQL 测试覆盖新增迁移及跨内容评论隔离、草稿可见性、审核和点赞。

## 宝塔部署顺序

本版本需要先应用新增迁移 `202609090003_update_comments.sql`，再启动新后端，最后发布新前端。新安装使用更新后的 schema.sql 和 init-db.js。

**升级时保留服务器所有已执行的历史 SQL 文件**，尤其是先前从备份恢复的 `202607220001_create_early_access_applications.sql`。只增加这一个新迁移，不以仓库目录覆盖服务器迁移目录，也不要导入 schema.sql。

升级前备份数据库、后端代码和前端入口；先运行迁移 dry-run 核验校验值。后端运行时变更为 `server/src/routes/comments.js`、`server/src/routes/admin.js`、`server/src/lib/mailer.js`；依赖未改变，无需重装生产依赖。前端使用构建后的 dist，保留旧哈希资源，最后替换 index.html。

上线后验证近况评论提交与审核、文章评论回归、匿名访问草稿被拒绝，以及手机 7/30 日横向滑动。
