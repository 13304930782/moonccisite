# 后台列表与媒体库容量修复（第一批）

## 本批确认的问题与修复

- P2：媒体库每次打开都全目录逐文件 stat、逐文件查询。现在使用目录流，每 500 个名字批量查询，仅为未登记文件读取元数据；并发请求共用扫描，成功后 60 秒内复用结果。站内上传仍立即登记，外部手工放入的文件最多延迟约 60 秒后在下次列表请求时发现。导入使用 INSERT IGNORE，避免竞争时覆盖已登记媒体或复活回收站条目。
- P2：媒体接口固定前 500 条，前端只在这部分搜索。新客户端使用服务端分页，每页 50 条（接口上限 100），搜索覆盖文件名、显示名、Alt 和原始文件名；状态/搜索变化回第一页并清空选择；迟到响应不得覆盖新查询。
- P2：后台文章列表无分页且返回正文。新客户端分页，每页 50 篇；查询只投影列表字段，编辑者范围和总数均限定本人。删除后页码超界会回到最后有效页。
- P2：后台首页为计数下载完整列表，评论总数受旧接口 300 条上限影响。新增 /admin/stats，仅返回数量；编辑者仅得到本人文章数，其他统计为 0，普通用户拒绝访问。
- 兼容已打开的旧页面：不传 page/pageSize 的媒体接口仍返回最多 500 条数组；文章旧接口仍返回精简字段数组。新客户端不依赖这些兼容入口。

## 测量和验证

- 10,000 个真实临时目录文件、模拟数据库响应：20 次批量查询；原逐文件算法需要 10,000 次。三个并发请求及缓存期内重复请求仍合计 20 次。一次本机扫描耗时约 53 ms，仅用于记录测试环境，不是生产延迟承诺。
- 隔离 MySQL：1,001 条媒体验证跨页无重复、查找第 500 条以外记录、超界页修正、匿名禁止访问与旧数组协议。
- 隔离 MySQL：124 篇编辑者文章，每篇 20 KB 正文。新第一页响应约 9.7 KB；旧正文部分已达 2.48 MB。验证文章列表不含正文、作者隔离和统计权限。
- 浏览器：统计不再下载列表；媒体/文章翻页、搜索回首页和迟到搜索响应隔离通过。对应脚本加入 CI。
- 未运行生产压测；本批没有为未经测量的 SQL 添加索引，也没有数据库结构变更。用户、评论管理列表本身的分页仍留待下一批，不宣称所有后台容量问题已经完成。

## 部署

前置：已完成近况评论版本的后端部署。此批更新四个后端文件和前端资源，重启 mooncci-api；不更新依赖、不执行迁移、不修改历史 SQL、.env 或上传目录。使用 scripts/deploy-media-capacity.sh <完整提交号>，通过 nohup 在后台执行。脚本构建后备份原路由和 index，先切换 API、健康检查通过后才发布前端；保留旧哈希资源，失败恢复原路由和入口。保留日志与备份，最终以部署退出码 0 为准。


## 用户与评论列表（第二批）

用户管理支持用户名/邮箱搜索、角色和状态筛选；评论管理支持关键词、审核状态和文章/近况来源筛选。分页每页 50 条，接口上限 100 条，越界页自动校正。旧客户端不传分页参数时保留数组响应。

前端忽略过期请求，审核后重新加载当前筛选结果，末页清空时自动返回有效页。停用用户保留文章和评论；普通管理员不能停用站长。评论 IP 仍仅向站长返回原始值。

验证包括隔离 MySQL 的 55 个用户、351 条评论分页/筛选/权限测试，以及浏览器翻页、搜索乱序响应、末页审核和来源筛选检查。无数据库迁移或依赖变更。
