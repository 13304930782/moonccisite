# mooncci 多宿舍电量管理

## 使用入口

后台 → 水电监控设置。顶部选择当前宿舍，右侧“添加宿舍 / 批量导入 / 宿舍与成员”按需展开，编辑时集中显示当前表单。原有运行计划、数据区和手动同步保留；手机端顺序排列，沿用黑白主题。

朋友先注册网站账号。站长添加宿舍时填写宿舍名称、学校查询账号 account、宿舍校验凭据 roomVerify，绑定朋友的网站用户名。无需给朋友后台管理员权限。一个宿舍可绑定多位成员，使用同一电表时只采集一次。

单个宿舍点击“验证并添加”。批量导入使用 JSON 数组，每批最多 10 个，先校验、预览，再确认导入。所有行验证成功才写入数据库，重复电表或已存在凭据不会覆盖原宿舍。输入框中的凭据保存后清空，接口不回显。

```json
[
  {"name":"朋友的宿舍","account":"学校查询账号","roomVerify":"宿舍校验凭据","members":["朋友的网站用户名"],"notifyTo":""}
]
```

account/roomVerify 沿用现在学校接口需要的查询凭据，并非让朋友提交网页登录密码。不要把真实凭据发到聊天或公开仓库。新宿舍的通知邮箱留空时不发送邮件；不会回退到站长的默认邮箱。在该宿舍“通知与阈值”中设置收件人并启用早晚简报。

朋友登录后访问 /electricity。只会列出已绑定且启用的宿舍；有多个宿舍时可切换。每个成员独立创建、重置对应宿舍的 RSS 链接。移除成员、停用账号或停用宿舍后链接失效，重新加入不会复活已撤销的令牌。

原有宿舍固定为仅站长可见，不允许绑定普通成员。所有电量面板/当前数据/历史接口均验证登录及宿舍权限，匿名请求返回 401。RSS 保留令牌访问，以支持阅读器，但每次读取核对账号和宿舍权限。

## 数据与采集

新增 rooms、room_members、room_state、room_runs 表。原实时快照的唯一键改为 scope_key + snapshot_date；历史日明细、零点预测、早晚报告沿用原来的范围键。原账号迁移保留 scope_key，历史与 RSS 令牌不重建；未带范围的旧快照归入原宿舍。

学校查询凭据使用 AES-256-GCM 加密，绑定宿舍 ID；密钥独立存于服务端 ELECTRICITY_CREDENTIAL_KEY，不进入浏览器。已有密钥不能随意更改；同时备份数据库和 .env。更换账号凭据必须指向同一电表，否则要求新建宿舍，避免旧历史归入新电表。

worker 每 15 秒检查各宿舍计划，顺序执行。保存计划仅从后续时间点生效；固定零点仍执行完整日统计与历史同步。宿舍/日期/时刻唯一任务记录防止重复执行；可重试失败任务至少间隔 15 分钟、最多 3 次，并继续沿用采集异常当日暂停机制。邮件重试读取已有报告，邮件失败不影响 RSS。

所有带宿舍上下文的学校请求通过数据库共享锁排队，每秒最多启动一次，API 和 worker 共用限制。后台验证入口每个站长每分钟最多 6 次提交，批量最多 10 个宿舍；手动历史冷却按宿舍持久保存。一个宿舍失败不暂停其他宿舍。

## 部署

用于已经部署最新电量 RSS / 日用电版本的现有项目。本版需要迁移 202609090001_electricity_rooms.sql，不要只同步 dist。无新增 npm 依赖。

电脑 PowerShell：

```powershell
Set-Location 'C:\Users\Administrator\Documents\mooncci site'
scp .cache/mooncci-electricity-rooms-20260909-v1.tar.gz root@182.92.179.81:/root/
```

服务器 root SSH：

```bash
mkdir -p /root/mooncci-electricity-rooms-20260909-v1
tar -xzf /root/mooncci-electricity-rooms-20260909-v1.tar.gz -C /root/mooncci-electricity-rooms-20260909-v1
bash /root/mooncci-electricity-rooms-20260909-v1/deploy-electricity-rooms.sh
```

脚本检查运行目录与包校验和，暂时停止 API/worker，备份源码、原入口、.env 与电量表数据/结构；生成或保留加密密钥；运行项目迁移器及原宿舍迁移；发布前端，重启 API/worker，验证健康检查与匿名电量 API 为 401。会有短暂维护时间，前端静态文件仍可读取。迁移失败时停止 API/worker，避免恢复旧公开接口；根据报错修复后重新执行，不要删除加密密钥。备份保存在终端输出的 /www/backup/mooncci-electricity-rooms-*，包含迁移前 .env 和含加密密钥的 .env 副本。数据库 JSON 备份仅供受控恢复，不要公开分享。

源码正常启动方式保持不变：前端 npm install / npm run dev / npm run build；后端在 server 下 npm install / npm start，worker 使用原 PM2 mooncci-worker 配置。

单独补同步时使用后台按钮；CLI 可通过 `node scripts/sync-electricity-history.js --room-id 宿舍ID` 指定宿舍。不传 ID 默认选择原有站长宿舍。

## 验证与边界

17 项针对性测试通过。隔离的真实 MySQL + Express 多宿舍集成测试覆盖旧数据迁移、密文保存、批量预览与失败无部分写入、同日电量隔离、异步凭据隔离、独立历史与状态、收件人隔离、邮件失败后 RSS 可读、重试不重复采集、工作队列串行、匿名/越权拒绝、成员移除/停用/重置令牌。现有 RSS/零点预测的真实 MySQL 集成测试继续通过。

本地浏览器用隔离数据库和模拟学校/邮件验证批量预览与确认、新宿舍切换、朋友可见列表、原有宿舍权限拒绝、匿名登录提示和手机深色表单。未连接生产数据库，未向真实学校账号发起测试请求，未发送真实邮件，也未声称 iOS 真机实测。上线后的真实学校验证在站长提交“验证并添加”时执行。

## 页面设计记录

保留现有黑白主题、页面导航、图表动画和逐行运行计划。增加顶部宿舍选择与三个管理入口，复杂凭据表单按需显示；日常视图仍以当前数据、运行计划、通知设置为主。原有宿舍仅站长可见，新宿舍按成员授权，不增加独立账户体系。
